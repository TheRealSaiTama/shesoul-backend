# Railway Production Configuration
spring.application.name=v1update
spring.main.web-application-type = servlet

# Server Configuration
server.port=${PORT:8080}
server.address=0.0.0.0
server.forward-headers-strategy=native

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
management.health.defaults.enabled=true
management.endpoint.health.probes.enabled=true
management.health.livenessState.enabled=true
management.health.readinessState.enabled=true

# Database Configuration
# Using H2 for now, but you should switch to PostgreSQL for production
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.h2.console.enabled=false

# HikariCP connection pool
spring.datasource.hikari.maximum-pool-size=4
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# Logging
logging.level.root=INFO
logging.level.com.sheandsoul=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.springframework.boot.actuate=DEBUG

# Performance
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true

# JWT and other secrets
jwt.secret.key=${JWT_SECRET:ThisIsMySuperSecretKeyForSheAndSoulApplication}
gemini.api.key=${GEMINI_API_KEY:AIzaSyALByhJy-brEmLuWbOFj9TIC-2UrZY9vBY}
